<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ✅ INTERNET ACCESS -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- ✅ CHECK NETWORK STATE (recommended) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- ✅ OPTIONAL: if using WiFi state -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <!-- ✅ OPTIONAL: if your app needs to change network (rare) -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>

    <!-- ✅ OPTIONAL: if using HTTP (non-HTTPS APIs) -->
    <application
        android:label="uje"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">  <!-- ⚠️ Needed for HTTP APIs -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>